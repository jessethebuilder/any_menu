<div id="menu_list">
  <h3>Menus</h3>
  <ul>
    <% menus.each do |m| %>
        <li>
          <%= link_to m.name, edit_menu_path(m) %>
        </li>
    <% end %>
  </ul>
   <%= link_to 'Add Menu', new_menu_path, :class => 'btn btn-success form-control' %>
</div>