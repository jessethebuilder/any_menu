<div class="container">
  <h2>Complete Order
  <small><%= link_to '(return to menu)', menu_path(store.current_menu) %></small></h2>

  <%= render :partial => 'form' %>
</div>

<script>
  function hideCurrentOrder(){
    $('#current_order').detach();
  }

  $(document).ready(hideCurrentOrder());
  $(document).on('page:load', hideCurrentOrder());
</script>

<!--todo when last item is destroyed, it creates an error, becuase stuff becomes nil-->